/*
Problem : Take four parameters. Multiply the four numbers and then return the result
*/

// Solution :
function multiplication(a, b, c, d) {
  const mul = a * b * c * d;
  return mul;
}
const result = multiplication(2, 4, 6, 8);
console.log(result);
